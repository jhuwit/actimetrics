% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acti_threshold_counts.R
\name{acti_threshold_counts}
\alias{acti_threshold_counts}
\title{Classify activity counts using intensity thresholds}
\usage{
acti_threshold_counts(
  data,
  thresholds,
  labels,
  name = "activity_intensity",
  epoch = 60
)
}
\arguments{
\item{data}{A data frame with \code{time} and \code{counts} columns.}

\item{thresholds}{Increasing numeric lower bounds for successive intensity
categories, expressed in counts per \code{epoch} seconds.}

\item{labels}{Labels for the categories.  There must be one more label than
threshold.}

\item{name}{Name of the factor column to add to \code{data}.}

\item{epoch}{Required epoch, in seconds.}
}
\value{
\code{data} with an ordered factor column named by \code{name}.
}
\description{
Applies count thresholds to a count-data set after verifying that its
timestamps have the requested, regular epoch.  Thresholds must be expressed
in counts per \code{epoch} seconds.  Published cut-points should only be used at
the epoch and placement for which they were developed.
}
\examples{
counts <- data.frame(
  time = as.POSIXct("2020-01-01", tz = "UTC") + 60 * 0:2,
  counts = c(0, 3000, 5000)
)
acti_threshold_counts(
  counts,
  thresholds = c(2860, 3941),
  labels = c("sedentary", "light", "mvpa")
)
}
