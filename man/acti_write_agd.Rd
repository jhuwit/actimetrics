% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acti_write_agd.R
\name{acti_write_agd}
\alias{acti_write_agd}
\title{Write ActiGraph Epoch Data to an AGD File}
\usage{
acti_write_agd(
  data,
  file,
  epoch_length = NULL,
  original_sample_rate = NA_integer_,
  settings = character(),
  overwrite = FALSE
)
}
\arguments{
\item{data}{A data frame with a time column and three count columns.
Time may be called \code{time}, \code{timestamp}, or \code{HEADER_TIMESTAMP}; count
columns may be \code{axis1}--\code{axis3} or \code{X}--\code{Z}.}

\item{file}{Path for the new \code{.agd} file.}

\item{epoch_length}{Length of each epoch in seconds.  Defaults to \code{NULL},
in which case it is estimated as the median spacing between timestamps.}

\item{original_sample_rate}{Sample rate of the signal used to derive the
counts.}

\item{settings}{Optional named character vector of additional ActiLife
settings.  Entries supplied here replace the defaults.}

\item{overwrite}{Whether to replace an existing file.}
}
\value{
\code{file}, invisibly.
}
\description{
Writes epoch-level axis counts to an SQLite-backed \code{.agd} file that can be
read by \code{\link[actigraph.sleepr:read_agd]{actigraph.sleepr::read_agd()}}.  The AGD format stores time as .NET
ticks (100 ns since 0001-01-01), rather than as Unix time.
}
\examples{
\dontrun{
counts <- read.csv("72310_counts.csv.gz")
acti_write_agd(counts, file, original_sample_rate = 80)
actigraph.sleepr::read_agd("7231060sec.agd")
}
}
