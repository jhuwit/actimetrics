% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ab_calculate_counts.R, R/acti_count_measures.R
\name{acti_calculate_counts}
\alias{acti_calculate_counts}
\alias{acti_calculate_wear}
\alias{acti_calculate_nonwear}
\alias{acti_apply_cole_kripke}
\alias{acti_apply_tudor_locke}
\alias{acti_apply_sadeh}
\title{Process Count Data}
\usage{
acti_calculate_counts(
  data,
  epoch = 60L,
  resample = TRUE,
  lfe_select = FALSE,
  verbose = TRUE
)

acti_calculate_wear(
  data,
  method = c("choi", "troiano"),
  use_magnitude = TRUE,
  ...
)

acti_calculate_nonwear(
  data,
  method = c("choi", "troiano"),
  use_magnitude = TRUE,
  ...
)

acti_apply_cole_kripke(data)

acti_apply_tudor_locke(data, ...)

acti_apply_sadeh(data, ...)
}
\arguments{
\item{data}{A \code{data.frame} from \code{acti_calculate_counts} that has
columns \code{axis1-3} and \code{counts}}

\item{epoch}{epoch length in seconds.  Default is 60 seconds.
See \code{agcounts::calculate_counts()}}

\item{resample}{(recommended) resample the data to 30Hz using
\link[actibase:acti_resample]{actibase::acti_resample} vs. using the resampling method from
\link[agcounts:calculate_counts]{agcounts::calculate_counts}.}

\item{lfe_select}{Apply the Actigraph Low Frequency Extension filter.
See \code{agcounts::calculate_counts()}
higher values are higher levels of verbosity.}

\item{verbose}{print diagnostic messages.  Either logical or integer, where}

\item{method}{Method for detecting non-wear, either "choi" or "troiano",
corresponding to \code{actigraph.sleepr::apply_choi()} or
\code{actigraph.sleepr::apply_troiano()}}

\item{use_magnitude}{If \code{TRUE}, the magnitude of the vector
(axis1, axis2, axis3) is used to measure activity;
otherwise the axis1 value is used.}

\item{...}{additional arguments to pass to \code{actigraph.sleepr} function}
}
\value{
A \code{data.frame} of transformed data

A \code{data.frame} of transformed data
}
\description{
Process Count Data

Process Count Data
}
\note{
This calls the downstream wear-processing helpers used by
\code{actigraph.sleepr}
}
\examples{
\dontrun{
path = actiread::acti_example_gt3x()
ac = actiread::acti_read_gt3x(path)
out = acti_calculate_counts(ac)
}
data = actimetrics::acti_count_data
wear = actimetrics::acti_calculate_wear(data)
tro_wear = actimetrics::acti_calculate_wear(data, method = "troiano")
ck = actimetrics::acti_apply_cole_kripke(data)
tl = actimetrics::acti_apply_tudor_locke(ck)
sadeh = actimetrics::acti_apply_sadeh(ck)
}
